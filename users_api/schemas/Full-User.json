{
    "type": "object",
    "properties": {
        "nombre": {
            "type": "string",
            "maxLength": 100,
            "pattern": "^[A-Za-z0-9]{3,10}$"
        },
        "apellido": {
            "type": "string",
            "maxLength": 100,
            "pattern": "^[A-Za-z0-9]{3,10}$"
        },
        "email": {
            "type": "string",
            "maxLength": 100,
            "pattern": "^[A-Za-z0-9]{3,10}$"
        },
        "clave": {
            "type": "string",
            "maxLength": 100,
            "pattern": "^[A-Za-z0-9]{3,10}$"
        },
        "fecha_nacimiento": {
            "type": "string",
            "maxLength": 10,
            "format": "date",
            "example": "1990-01-01"
        }
    },
    "additionalProperties": false
}